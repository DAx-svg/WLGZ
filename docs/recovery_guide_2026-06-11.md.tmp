================================================================================
  数据恢复操作指南 — 写给另一台电脑的 AI
  生成时间：2026-06-11 01:10（修订版，新增完整数据库上传方案）
  请严格按步骤执行，不要跳过任何一步
================================================================================

关键发现：sync_db.py 只会推送物料主表（materials），不会推送出库记录、售后工单、
故障记录、版本变更。如果用 sync_db.py 推送，关联数据会全部丢失。

正确做法：通过新增的 /api/db/upload 接口，把完整数据库文件上传到云端。


一、发生了什么
================================================================================

2026-06-11，主电脑运行 sync_db.py 时云端数据库是空的（刚重新部署），同步脚本把空云端
当成了权威源，将本地真实物料全部删除、所有出库/售后/故障/版本变更记录永久丢失。

你那台电脑是恢复全部数据的唯一希望。


二、当前云端状态
================================================================================

- 云端下载 token 已被改为 wlgz-sync-2026-BLOCKED，同步脚本连上会收 HTTP 403 自动跳过
- 云端现存：5 条示例数据 + 23 条仅 SN 的空壳恢复数据（无关联记录）
- 主电脑已推送新版代码到 GitHub，新增了 /api/db/upload 完整数据库上传接口


三、你到公司后第一步（最紧急）
================================================================================

3.1 断开网络
----------------------------------------
拔网线或关 WiFi。物理断网最可靠。

3.2 立即备份
----------------------------------------
打开命令行（cmd），进入项目目录 d:\WLGZ\WLGZ，执行：

    copy material.db material.db.SAFE_20260611

确认备份里有完整数据（物料 > 5，出库/售后不为 0）：

    python -c "import sqlite3; db=sqlite3.connect('material.db.SAFE_20260611'); db.row_factory=sqlite3.Row; print('物料:', db.execute('SELECT COUNT(*) FROM materials').fetchone()[0], '条'); print('出库:', db.execute('SELECT COUNT(*) FROM outbound_records').fetchone()[0], '条'); print('售后:', db.execute('SELECT COUNT(*) FROM after_sales_records').fetchone()[0], '条'); print('故障:', db.execute('SELECT COUNT(*) FROM fault_records').fetchone()[0], '条'); print('版本变更:', db.execute('SELECT COUNT(*) FROM version_changes').fetchone()[0], '条')"

3.3 删除同步状态文件
----------------------------------------
    del sync_state.json

这个文件会导致同步脚本把本地独有的数据当成「在云端被删掉的」而全部删除。


四、更新云端代码（开启上传接口）
================================================================================

4.1 登录 PythonAnywhere
----------------------------------------
浏览器打开 https://www.pythonanywhere.com ，用跟主电脑同一个账号登录。

4.2 拉取最新代码并恢复 token
----------------------------------------
顶部点 Consoles → 打开 Bash console，执行：

    cd ~/WLGZ
    git pull origin master

这一步会把代码更新到最新版，同时恢复正确的下载 token（之前手动改的 BLOCKED 会被覆盖掉）。

验证：

    grep "wlgz-sync" app.py

应该看到 wlgz-sync-2026（不带 BLOCKED）。

4.3 Reload
----------------------------------------
顶部点 Web 标签 → 绿色 Reload 按钮。


五、上传完整数据库到云端（关键步骤）
================================================================================

5.1 恢复网络
----------------------------------------
把网线插回去或连上 WiFi。

5.2 拉取最新代码到本地
----------------------------------------
在项目目录 d:\WLGZ\WLGZ 执行：

    git pull origin master

5.3 执行上传
----------------------------------------
在项目目录 d:\WLGZ\WLGZ 执行以下 Python 脚本（复制整段，粘贴到命令行）：

python -c "
import urllib.request, os

DB = r'd:\WLGZ\WLGZ\material.db'
TOKEN = 'wlgz-sync-2026'
URL = 'https://daxsvg.pythonanywhere.com/api/db/upload'

print(f'数据库大小: {os.path.getsize(DB)/1024:.1f} KB')

with open(DB, 'rb') as f:
    db_data = f.read()

import io, uuid
boundary = uuid.uuid4().hex.encode()
body = (
    b'--' + boundary + b'\r\n'
    b'Content-Disposition: form-data; name=\"token\"\r\n\r\n'
    + TOKEN.encode() + b'\r\n'
    b'--' + boundary + b'\r\n'
    b'Content-Disposition: form-data; name=\"db\"; filename=\"material.db\"\r\n'
    b'Content-Type: application/octet-stream\r\n\r\n'
    + db_data + b'\r\n'
    b'--' + boundary + b'--\r\n'
)

req = urllib.request.Request(URL, data=body,
    headers={'Content-Type': 'multipart/form-data; boundary=' + boundary.decode()})
with urllib.request.urlopen(req, timeout=60) as resp:
    import json
    result = json.loads(resp.read())
    print(result.get('message', str(result)))
"

5.4 确认上传成功
----------------------------------------
看到「数据库已上传，物料 XX 条」说明成功。
在浏览器打开 https://daxsvg.pythonanywhere.com 确认数据完整。


六、主电脑恢复
================================================================================

云端数据完整后，主电脑这边执行：

    cd d:\WLGZ\WLGZ
    del sync_state.json
    python sync_db.py

就会把完整数据从云端拉回本地。


七、注意事项
================================================================================

1. 绝对不要在主电脑上跑 sync_db.py，直到你那台电脑先上传完毕

2. sync_state.json 是罪魁祸首 — 操作前一定先删掉

3. 上传完成后，在云端 Bash Console 跑一次备份：
   python ~/WLGZ/backup.py

4. 以后部署代码更新用 git pull，不要再 git clone，避免云端数据库被重置

5. 主电脑已经修了 4 个 Bug + 4 个逻辑设计问题，新增了 6 项功能：
   - 操作日志（全部操作审计追溯）
   - 数据导出 CSV（物料/出库/售后/故障/日志）
   - 客户信息复用（出库时下拉选择历史客户）
   - 物料状态时间线（全生命周期一览）
   - 库存盘点功能
   - 售后完成时可选返还客户或回库
   你那台电脑 git pull 最新代码就能用这些功能

6. 如果你那台电脑上之前设置了定时自动同步（每30分钟跑一次 sync_db.py），
   现在要去关掉！否则上传完它又会覆盖。检查方法：
   - Windows 任务计划程序 → 找 sync_db 相关任务 → 禁用
   - 或者命令行：schtasks /query | findstr sync


八、如果数据也有问题
================================================================================

立即停止一切操作。不要连上云端。
